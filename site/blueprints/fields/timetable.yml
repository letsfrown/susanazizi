type: structure
width: 1/3
sortBy: startdate desc
fields:
  startDate:
    type: date
    display: 'YYYY'
    label: Start
  endDate:
    type: date
    label: Ende
    display: 'YYYY'
    help: optional
  entry:
    extends: fields/text
    label: Eintrag

columns:
    startDate:
      mobile: true
      width: 1/3
    endDate:
      mobile: false
    entry:
      mobile: true
      width: 2/3
